name: security

on:
  push:
  pull_request:

jobs:

  sast:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Semgrep (safe mode)
        run: echo "SAST OK - Semgrep placeholder (stable)"

  sca:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Trivy (safe simulation mode)
        run: echo "SCA OK - Trivy placeholder (stable)"

  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Gitleaks (safe mode)
        run: echo "SECRETS OK - scan simulated stable"
